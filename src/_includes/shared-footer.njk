<footer>
<div class="wrap">
<p style="margin-bottom:.4rem">Met zorg gemaakt door Chantal &amp; het team — een vertrouwd adres in Veenendaal.</p>
{%- if page.url == "/solliciteren.html" %}
© 2026 Boslaantje Doen · Werken bij ons in Veenendaal e.o. · <a href="index.html">Naar de homepage</a> · <a href="veelgestelde-vragen.html">Veelgestelde vragen</a>
{%- else %}
© 2026 Boslaantje Doen · Snackbar in Veenendaal e.o. · <a href="index.html">Naar de homepage</a> · <a href="solliciteren.html">Werken bij ons</a> · <a href="veelgestelde-vragen.html">Veelgestelde vragen</a>
{%- endif %}
<!-- Subtiele verwijzing naar het nieuwe MaCA-label — placeholder-naam/link,
     invullen zodra de definitieve merknaam en het domein vaststaan. -->
<p class="foot-maca-note" style="display:none;margin-top:1rem;padding-top:1rem;border-top:1px solid rgba(0,0,0,.08);font-size:.85rem;opacity:.7">
Op zoek naar catering, verhuur of een zakelijk event? Dat verzorgen we voortaan via <a href="#">MaCA [Naam invulling] →</a>
</p>
</div>
</footer>

<script>
// mobiel hamburgermenu — zelfde betrouwbare geforceerde-reflow-aanpak als op de homepage
const navToggle = document.getElementById('navToggle');
const mobileMenu = document.getElementById('mobileMenu');
if (navToggle && mobileMenu) {
const setMenu = (open) => {
navToggle.setAttribute('aria-expanded', open ? 'true' : 'false');
navToggle.setAttribute('aria-label', open ? 'Menu sluiten' : 'Menu openen');
if (open) {
mobileMenu.hidden = false;
void mobileMenu.offsetHeight;
mobileMenu.classList.add('is-open');
} else {
mobileMenu.classList.remove('is-open');
window.setTimeout(() => { mobileMenu.hidden = true; }, 320);
}
};
navToggle.addEventListener('click', () => {
setMenu(navToggle.getAttribute('aria-expanded') !== 'true');
});
mobileMenu.querySelectorAll('a').forEach(a => a.addEventListener('click', () => setMenu(false)));
document.addEventListener('keydown', e => {
if (e.key === 'Escape' && navToggle.getAttribute('aria-expanded') === 'true') {
setMenu(false); navToggle.focus();
}
});
}
</script>
